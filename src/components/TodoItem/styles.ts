import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        borderBottomColor: "#000",
        borderBottomWidth: 2,
        backgroundColor: "#eee",
        alignItems: "center",
        height: 80
    },
    descriptionText: {
        flex: 1,
        fontSize: 22
    }
});